/*
const { When } = require('@cucumber/cucumber');
const Perf = require('../page-objects/pages/perf');

When('I run a lighthouse audit on the {string}', { timeout: 20 * 1000 }, async function (name)  {
  await Perf.audit(name);
});
*/
