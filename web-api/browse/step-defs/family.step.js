// const { Given, Then } = require('@cucumber/cucumber');
// const { FamilyApiAction } = require('../page-objects/pages/family-api-action');

// const action = new FamilyApiAction();

// // Browse Product
// // Family
// Given('Affiliate executes the family action with family ID {string} simple {string}', async function (fid, simple) {
//   await action.executeFamily(global.apiPid, fid, simple);
// });

// Then('Family with family ID {string} simple {string} should be displayed', async function (fid, simple) {
//   await action.familyValidate(global.apiPid, fid, simple);
// });
