Feature: Remove items from registry
    Scenario: Remove items from registry
    Scenario: Remove items from registry which was marked as Group gift
    # Fullfilled = wants == purchased
    Scenario: Should'nt able to Remove items from registry which was Fullfilled
    Scenario: Should'nt able to Remove items from registry which was partially Fullfilled
    # NLA = No longer available product
    Scenario: Remove item from registry which was NLA